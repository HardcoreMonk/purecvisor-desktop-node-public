# Desktop Node Follow-Up Queue

작성 기준: 2026-05-04, 독립 Windows repo 분리, Phase 19 제품 승격 재판정, Phase 25 .NET/TypeScript scaffold, .NET Windows Service Host replacement, internal enterprise `RequireSigned` MSI lifecycle smoke, `0.33.0-admin-smoke` Service/MSI/Hyper-V/firewall/trust-store 실제 host mutation smoke, Windows Server 2022 guest shutdown smoke 이후

이 문서는 `purecvisor-desktop-node` 저장소의 다음 작업 대기열만 정리한다. Phase별 완료 증거는 각 `docs/superpowers/plans/` 문서를 단일 진실로 둔다. 현재 적용 설계 결정은 `docs/ADR_INDEX.md`와 `docs/adr/`를 따르고, 검증 기준은 `docs/DEVELOPMENT_VERIFICATION_POLICY.md`를 따른다.

## 현재 상태

- `DESKTOP_NODE_REPOSITORY_DECISION: standalone-windows-repo`
- `DESKTOP_NODE_DOCS_DECISION: lightweight-adr-index`
- `PRODUCT_RUNTIME_PROMOTION_DECISION: ga-ready-product-runtime`
- `DESKTOP_NODE_SERVICE_DISTRIBUTION_DECISION: internal-only-service`
- `DESKTOP_NODE_PHASE19_PROMOTION_REDECISION: evidence-first-keep-spike`
- `DESKTOP_NODE_PHASE22_RELEASE_VERSION_DECISION: channel-version-artifact-policy-with-keep-spike`
- `DESKTOP_NODE_INTERNAL_SIGNING_DECISION: internal-root-leaf-requiresigned`
- `DESKTOP_NODE_PHASE25_SERVICE_HOST_REPLACEMENT: dotnet-windows-service-host-default-with-keep-spike`
- `DESKTOP_NODE_PUBLIC_DISTRIBUTION_DECISION_CANDIDATE: public-distribution-operations-expansion-candidate`
- Desktop Node Phase 1-18 구현/문서 이력을 새 Windows 전용 저장소로 분리했다.
- Phase 19는 evidence-first 재판정으로 `PRODUCT_RUNTIME_PROMOTION_DECISION: keep-spike`를 유지한다고 결론냈지만, 2026-05-05 ADR-0004가 aggregate closure와 internal stable evidence를 근거로 내부 전용 서비스 범위의 `ga-ready-product-runtime` 결정을 적용했다.
- Phase 20-23은 남은 GA 차단 gate를 signed release/MSI lifecycle, Hyper-V lifecycle integration, release/version policy, Windows operational evidence 단위로 나누어 runbook을 준비했다.
- Phase 22 후속 개발에서 installer `build.ps1`는 `windows-x64` MSI/provenance/hash sidecar naming, build plan/provenance `release_channel`, RC/stable unsigned build 차단을 강제한다.
- Phase 24 후보는 Local API job runtime public boundary를 먼저 고정하는 작업이다. 현재 TDD slice는 `GET /api/v1/runtime/policy`의 `job_runtime` contract, read-only `network.inventory`, running job recovery, future job store version quarantine, retry/cancel semantics, diagnostics bundle self-audit, CLI `runtime policy` consumer까지 완료했다.
- Phase 23 후속 개발에서 diagnostic bundle은 `operational-evidence-redacted.json`으로 SCM failure action, service log retention, Event Log deferred policy, host mutation 미수행 여부를 요약한다.
- Phase 25 후보는 .NET contract/runtime core, TypeScript Web Console, PowerShell Windows adapter 역할 분리를 문서화했고 구현 slice인 `.NET contract mirror`, 순수 .NET job state transition validator, TypeScript Web Console static parity scaffold/generated manifest/verification/regeneration/user-visible fixture/browser fixture flow, .NET API/service host 및 handler/lifecycle adapter candidate contract를 `src/DesktopNode.Contracts/**`, `src/DesktopNode.Runtime/**`, `src/DesktopNode.Api/**`, `src/DesktopNode.Service/**`, `web/src/**`, `web/generated/parity/**`, `web/scripts/**`와 tests/typecheck로 추가했다. 이후 `src/DesktopNode.Host/**`를 추가해 기본 제품 service host, listener owner, SCM binary path, MSI installed custom action runner를 `DesktopNode.Host.exe`로 교체했다. C# native adapter slices로 `host.status`, `network.inventory`, `vm.list`, `GET /api/v1/vms/{id}`, `GET /api/v1/vms/{id}/checkpoints` read route는 helper fallback 없이 native structured success/failure를 반환한다. VM create/start/shutdown/poweroff/restart/delete는 .NET request processor queue를 유지하되 C# WMI/native Hyper-V adapter가 직접 실행한다. Native VM create product path는 Generation 2만 지원하고 Generation 1 request는 `PCV_GENERATION_INVALID` structured failure로 반환한다. Native VM delete는 managed marker guard와 missing VM idempotent `action=absent` contract를 둔다. Checkpoint create/restore/delete는 .NET request processor queue를 유지하되 C# WMI snapshot service adapter가 직접 실행한다. 2026-05-04 service product ops slice는 `DesktopNode.Host.exe service-action status|start|stop|configure-installed|repair-installed|remove-installed|data-root-remove`를 native SCM/data-root action 중심으로 확장했고, protected token ACL hardening에서 외부 `icacls.exe` 호출을 제거했다. `remove-installed --remove-data`는 직접 ProgramData를 지우지 않고 `data-root-remove` handoff를 반환하며, `data-root-remove --remove-data`는 service absent precondition과 allowlist deletion만 수행한다. `artifacts/service-action-status-start-stop-20260504-002359`에서 installed status/stop/start/status, runtime policy health `200`, final service `Running`, boot time unchanged를 확인했다. Job store unsupported future schema는 quarantine/move 대신 409 blocked diagnostics/no-mutation contract로 전환했다. Runtime policy는 `dispatch.native_probe_operations=[host.status,network.inventory,vm.list,checkpoint.list]`, `dispatch.native_mutation_operations=[vm.create,vm.start,vm.shutdown,vm.poweroff,vm.restart,vm.delete,checkpoint.create,checkpoint.restore,checkpoint.delete]`, `dispatch.mutation_dispatch=native-vm-create-lifecycle-delete-checkpoint-mutation`을 노출한다. `0.30.1-admin-smoke` 설치본 mutation smoke는 service-action, MSI lifecycle, installed Hyper-V API route, VM create/start/restart/poweroff/delete, checkpoint create/restore/delete native mutation, managed delete `action=delete`, repeat delete `action=absent`, unmanaged guard block, installer-ISO shutdown unavailable structured failure를 자동 reboot 없이 재확인했다. `artifacts/guest-shutdown-windows-smoke-20260503-222750`은 Microsoft Windows Server 2022 Evaluation VHD guest에서 installed Local API `vm.shutdown` job `succeeded`, final VM `Off`, cleanup 완료를 확인했다. VM summary/storage/network native parity code-level slices는 WMI CPU/startup memory/generation/checkpoint count, storage path, network switch mapping을 추가했다.
- Draft PR ready 직접 gate는 signed MSI lifecycle, Hyper-V product-flow, release approval, firewall cleanup, 운영/Event Log lifecycle evidence 기준으로 닫혔다. 2026-05-01 관리자 opt-in hardening smoke에서 WinSW service reinstall/start, SCM failure action apply, protected token ACL inspection, firewall create/update/delete, Event Log scoped source lifecycle, LAN listener/firewall preview, direct Hyper-V lifecycle, Product API Hyper-V lifecycle, self-signed TLS reverse proxy preview, 75초 운영 sampling도 통과했다. 같은 날 current-head `3d35aa2` 기준 `0.23.9-rc.1` local test `RequireSigned` MSI lifecycle과 product-wrapper update/rollback/config migration smoke도 통과했고, internal Root/leaf signer 기준 `0.23.10-rc.1` internal enterprise `RequireSigned` MSI lifecycle도 통과했다. 최신 관리자 opt-in 보강 evidence `artifacts/admin-optin-hyperv-service-msi-firewall-eventlog-20260501-185911`는 자동 reboot 비활성/미관측 상태에서 internal enterprise `RequireSigned` MSI lifecycle, service final restore, web root, Hyper-V lifecycle/checkpoint list, firewall cleanup, Event Log source lifecycle을 재확인했다. `artifacts/dotnet-host-admin-smoke-20260501-213444`는 `DesktopNode.Host.exe` direct service-action, MSI lifecycle, Hyper-V helper integration smoke를 자동 reboot 없이 통과했다. `artifacts/routeparity-service-msi-hyperv-mutation-20260502-012126`는 tracked route parity mutation runner로 `0.26.8-admin-smoke` service-action, MSI lifecycle, 설치본 .NET Host Hyper-V API route smoke, C# native `network.inventory` response를 자동 reboot 없이 통과했다. Local self-signed/root trust 우회와 TLS preview는 test host workaround이고, internal enterprise evidence는 내부 서비스 운영용이다. `0.26.x-admin-smoke`는 unsigned admin-smoke evidence다. public trusted signing evidence와 stable publication은 별도 판단이다.
- `docs/ADR_INDEX.md`, `docs/adr/0001-standalone-windows-repo-and-evidence-first-keep-spike.md`, `docs/adr/0002-release-version-policy.md`, `docs/adr/0003-internal-trusted-signing-policy.md`가 현재 Desktop Node 결정 진입점이다.
- `AGENTS.md`는 ADR index, 문서 진입점, 검증 원칙 중심으로 정리됐다.
- Linux `purecvisor-single` 런타임과 Single Edge 릴리스 판단은 이 저장소의 대상이 아니다.
- 제품 wrapper는 `packaging/windows-desktop-node/**`에 있고, 기본 service host는 `src/DesktopNode.Host/**`의 `DesktopNode.Host.exe`다. Component 자산은 `archive/spikes/purecvisor-desktop-node/**`에 있다.
- GA-ready 제품 재설계는 `docs/superpowers/specs/2026-05-02-purecvisor-desktop-node-ga-ready-redesign-design.md`와 ADR-0004에 있으며, ADR-0001의 `keep-spike` 제품 승격 판단을 대체한 현재 적용 결정이다. 범위는 내부 전용 서비스이며 public trusted signing과 외부 stable publication은 scope 밖이다.
- 2026-05-07 후속 재점검에서 packaging/distribution future phase와 Operator/Web UX 확장을 분리했다. Burn bootstrapper, MSIX, winget manifest, network download updater, full transactional rollback, Windows Credential Manager transition, default Windows Event Log writer/provider transition, built-in TLS certificate lifecycle, service token rotation/revoke mutation API, diagnostic bundle server-side generation/download는 대부분 `future-noncurrent`이며 별도 spec/plan/ADR 또는 release approval 없이 현재 closure blocker나 완료 항목으로 세지 않는다. Network download updater는 source gate와 catalog/channel resolver code-level partial만 구현됐다. Operator/Web UX P0/P1/P2, VM delete UI, Web Dashboard Ops Cockpit plan, Network Inventory Web View, Diagnostic Bundle operator handoff UI는 closure 상태이고 남은 UX 후보는 새 backlog로만 추적한다. Evidence는 `docs/ga-ready/evidence/packaging-operator-backlog-rebaseline-2026-05-07.md`, `docs/ga-ready/evidence/web-console-network-inventory-view-2026-05-07.md`, `docs/ga-ready/evidence/web-console-diagnostic-bundle-ui-2026-05-07.md`다.
- 2026-05-07 후속 slice에서 network download updater의 source gate code-level partial을 구현했다. `Invoke-PcvDesktopNodeProduct.ps1 -Action Update`는 `-SourceUri`, `-ExpectedSha256`, `-DownloadRoot`로 file/HTTPS ZIP package를 service stop 전에 staging/extract하고 SHA-256을 검증한다. `http://` source는 `PCV_PRODUCT_UPDATE_SOURCE_URI_UNTRUSTED`로 차단된다. 이는 full updater, external channel/catalog, public trusted signing, 외부 stable publication, installed destructive update/rollback smoke 완료 evidence가 아니다. Evidence는 `docs/ga-ready/evidence/network-download-update-source-gate-2026-05-07.md`다.
- 2026-05-07 후속 slice에서 updater catalog/channel resolver code-level partial을 구현했다. `Invoke-PcvDesktopNodeProduct.ps1 -Action Update`는 `-UpdateCatalogUri`, `-UpdateChannel`, `-DownloadRoot`로 file/HTTPS JSON catalog schema v1을 service stop 전에 해석하고 selected channel의 version/package URI/SHA-256을 기존 source gate로 넘긴다. Missing channel은 `PCV_PRODUCT_UPDATE_CATALOG_CHANNEL_NOT_FOUND`, unsupported schema는 `PCV_PRODUCT_UPDATE_CATALOG_SCHEMA_UNSUPPORTED`, direct source와 catalog 동시 지정은 `PCV_PRODUCT_UPDATE_SOURCE_CONFLICT`로 차단된다. 이는 external publication service, public trusted signing, 외부 stable publication, installed destructive catalog update smoke 완료 evidence가 아니다. Evidence는 `docs/ga-ready/evidence/full-updater-catalog-channel-2026-05-07.md`다.
- 2026-05-07 후속 slice에서 full transactional rollback future phase의 transaction journal diagnostics code-level partial을 구현했다. `Invoke-PcvDesktopNodeProduct.ps1 -Action Update`는 payload validation 직후 service stop 전에 `%ProgramData%\PureCVisor\desktop-node\update-transaction.json`을 쓰고, `update-transaction.begin`, stage transition, success/`failed-rolled-back`, rollback result, structured `PCV_*` error를 기록한다. Diagnostic bundle은 existing journal을 `update-transaction-journal-redacted.json`으로 포함한다. 이는 full transactional filesystem rollback, post-crash resume/reconcile, public trusted signing, 외부 stable publication, installed destructive update/rollback smoke 완료 evidence가 아니다. Evidence는 `docs/ga-ready/evidence/update-transaction-journal-diagnostics-2026-05-07.md`다.
- 2026-05-07 후속 slice에서 product root filesystem rollback code-level partial을 추가했다. `Invoke-PcvDesktopNodeProduct.ps1 -Action Update`는 product root backup 이후 copy/config/start/health failure에서 previous root restore를 시도하고, journal에 `failed-rolled-back` 또는 `failed-rollback-failed`, `rollback_result`, original `PCV_*` error를 기록한다. 이는 post-crash resume/reconcile, service/data/config/job-store transaction manager, public trusted signing, 외부 stable publication, installed destructive catalog/update smoke 완료 evidence가 아니다. Evidence는 `docs/ga-ready/evidence/full-transactional-filesystem-rollback-2026-05-07.md`다.
- 2026-05-07 후속 slice에서 packaging/publication descriptor code-level partial을 추가했다. Installer `build.ps1`는 MSI/provenance/hash sidecar와 함께 `PureCVisorDesktopNode-<version>-windows-x64.publication.json`을 작성하고, `internal-artifact-descriptor-only`, public trusted signing/external stable publication `not-claimed`, Burn/MSIX `not-built`, winget `not-generated`, catalog publication `not-published`를 기록한다. 이는 Burn bootstrapper, MSIX, winget submission, external publication service, public trusted signing, 외부 stable publication 완료 evidence가 아니다. Evidence는 `docs/ga-ready/evidence/packaging-publication-descriptor-2026-05-07.md`다.
- 2026-05-07 ADR-0005 1차 PR은 `public-distribution-operations-expansion-candidate` 후보를 추가한다. `docs/ga-ready/PUBLIC_DISTRIBUTION_GATE_MATRIX.md`와 `packaging/windows-desktop-node/tools/New-PcvPublicDistributionDescriptor.ps1` dry-run descriptor는 public trusted signing/external stable publication, Burn bootstrapper, MSIX, winget manifest, updater catalog publication, public signed update/rollback smoke, Windows Credential Manager/Event Log/TLS 운영 확장 gate를 `not-claimed` 상태로 추적한다. Evidence는 `docs/ga-ready/evidence/public-distribution-operations-expansion-phase1-2026-05-07.md`이며 실제 host mutation은 수행하지 않는다.
- 2026-05-07 후속 public distribution readiness preflight는 `packaging/windows-desktop-node/tools/New-PcvPublicDistributionReadiness.ps1`를 추가해 publication descriptor에서 winget manifest preview와 `winget validate` manual follow-up을 산출한다. `winget_submission=not-submitted`, `host_mutation_performed: false`, public trusted signing/external stable publication `not-claimed`이며 evidence는 `docs/ga-ready/evidence/public-distribution-readiness-preflight-2026-05-07.md`다.
- 2026-05-08 후속 winget manifest compliance preflight는 `packaging/windows-desktop-node/tools/New-PcvWingetManifestCompliancePreflight.ps1`를 추가해 생성된 winget singleton manifest preview를 offline compliance로 검증한다. `validation_status: offline-compliance-pass`, `winget_submission: not-submitted`, `actual_execution: not-run`, `host_mutation_performed: false`, public trusted signing/external stable publication `not-claimed`이며 evidence는 `docs/ga-ready/evidence/winget-manifest-compliance-preflight-2026-05-08.md`다.
- 2026-05-07 후속 updater catalog publication preflight는 `packaging/windows-desktop-node/tools/New-PcvUpdaterCatalogPublicationPreflight.ps1`를 추가해 selected HTTPS catalog channel에서 catalog publication preview와 SHA-256 sidecar를 산출한다. `catalog_publication: not-published`, `actual_execution: not-run`, `host_mutation_performed: false`, public trusted signing/external stable publication `not-claimed`이며 evidence는 `docs/ga-ready/evidence/updater-catalog-publication-preflight-2026-05-07.md`다.
- 2026-05-08 후속 public signed update/rollback smoke preflight는 `packaging/windows-desktop-node/tools/New-PcvPublicSignedUpdateRollbackSmokePreflight.ps1`를 추가해 selected catalog channel에서 clean-host smoke plan preview를 산출한다. `public_signed_update_rollback_smoke: blocked-by-public-signing-and-publication`, `clean_host_smoke_status: not-run`, `actual_execution: not-run`, `host_mutation_performed: false`, public trusted signing/external stable publication `not-claimed`이며 evidence는 `docs/ga-ready/evidence/public-signed-update-rollback-smoke-preflight-2026-05-08.md`다.
- 2026-05-08 후속 Windows Credential Manager transition preflight는 `packaging/windows-desktop-node/tools/New-PcvWindowsCredentialManagerTransitionPreflight.ps1`를 추가해 service name, credential target, 현재 DPAPI protected token file storage, 목표 Windows Credential Manager storage, transition plan preview를 산출한다. Token value read, credential write/delete, service reload, host mutation은 실행하지 않으며 `credential_manager_transition: blocked-by-no-mutation-preflight`, `credential_manager_mutation: not-run`, `token_value_observed: false`, `actual_execution: not-run`, `host_mutation_performed: false`, public trusted signing/external stable publication `not-claimed`다. Evidence는 `docs/ga-ready/evidence/windows-credential-manager-transition-preflight-2026-05-08.md`다.
- 2026-05-08 후속 Windows Event Log provider transition preflight는 `packaging/windows-desktop-node/tools/New-PcvWindowsEventLogProviderTransitionPreflight.ps1`를 추가해 service name, provider name, log name, 현재 JSONL-first/Event Log opt-in writer policy, 목표 default Windows Event Log provider writer, provider transition plan preview를 산출한다. Provider registration/removal, event write/query, default writer switch, host mutation은 실행하지 않으며 `event_log_provider_transition: blocked-by-no-mutation-preflight`, `event_log_provider_mutation: not-run`, `event_log_write_status: not-run`, `actual_execution: not-run`, `host_mutation_performed: false`, public trusted signing/external stable publication `not-claimed`다. Evidence는 `docs/ga-ready/evidence/windows-event-log-provider-transition-preflight-2026-05-08.md`다.
- 2026-05-08 후속 built-in TLS certificate lifecycle preflight는 `packaging/windows-desktop-node/tools/New-PcvBuiltinTlsCertificateLifecyclePreflight.ps1`를 추가해 service name, certificate subject, HTTPS bind prefix, 현재 TLS mode, 목표 built-in service certificate mode, TLS lifecycle plan preview를 산출한다. Private key material 생성, certificate import/export, trust-store mutation, HTTPS/LAN binding, host mutation은 실행하지 않으며 `tls_certificate_lifecycle: blocked-by-no-mutation-preflight`, `tls_certificate_mutation: not-run`, `private_key_material_created: false`, `trust_store_mutation: not-run`, `lan_binding_mutation: not-run`, `actual_execution: not-run`, `host_mutation_performed: false`, public trusted signing/external stable publication `not-claimed`다. Evidence는 `docs/ga-ready/evidence/builtin-tls-certificate-lifecycle-preflight-2026-05-08.md`다.
- 2026-05-08 후속 service token rotation/revoke preflight는 `packaging/windows-desktop-node/tools/New-PcvServiceTokenRotationRevokePreflight.ps1`를 추가해 service name, protected token path, 현재 DPAPI protected token file storage, rotation mode, Service token rotation revoke plan preview를 산출한다. Token value read, new token generation, protected token write, service reload, old-token rejection verification, audit record write, host mutation은 실행하지 않으며 `service_token_rotation_revoke: blocked-by-no-mutation-preflight`, `service_token_mutation: not-run`, `service_token_value_observed: false`, `new_token_value_created: false`, `service_reload_status: not-run`, `old_token_rejection_status: not-run`, `token_rotation_audit_status: not-run`, `actual_execution: not-run`, `host_mutation_performed: false`, public trusted signing/external stable publication `not-claimed`다. Evidence는 `docs/ga-ready/evidence/service-token-rotation-revoke-preflight-2026-05-08.md`다.
- 2026-05-08 후속 diagnostic bundle server-side preflight는 `packaging/windows-desktop-node/tools/New-PcvDiagnosticBundleServerPreflight.ps1`를 추가해 service name, diagnostics root, Local API generation route, download route template, bearer authorization policy, redaction policy, retention policy, Diagnostic bundle server-side plan preview를 산출한다. Local API action execution, archive creation, download serving, redaction execution, retention application, product diagnostics runner delegation, host mutation은 실행하지 않으며 `diagnostic_bundle_server_generation: blocked-by-no-mutation-preflight`, `diagnostic_bundle_api_action: not-run`, `diagnostic_bundle_archive_created: false`, `diagnostic_bundle_download_served: false`, `diagnostic_bundle_redaction_status: not-run`, `diagnostic_bundle_authz_status: not-run`, `diagnostic_bundle_retention_status: not-run`, `actual_execution: not-run`, `host_mutation_performed: false`, public trusted signing/external stable publication `not-claimed`다. Evidence는 `docs/ga-ready/evidence/diagnostic-bundle-server-preflight-2026-05-08.md`다.
- 2026-05-08 후속 diagnostic bundle server code-level slice는 `DesktopNodeApiRequestProcessor`에 `POST /api/v1/diagnostics/bundles`와 `GET /api/v1/diagnostics/bundles/{bundle_id}/download`를 추가하고, `DesktopNode.Host.exe listen` service plan에 `--diagnostics-root`를 연결했다. API test는 redacted `.bundle.json`, `[REDACTED]`, `X-PCV-Diagnostic-Bundle-Id`, max-count retention을 확인한다. Installed listener execution, product wrapper diagnostics delegation, host mutation은 실행하지 않으며 `diagnostic_bundle_server_generation: partial-code-level-api-action`, `diagnostic_bundle_api_action: code-level-applied`, `diagnostic_bundle_archive_created: code-level-created`, `diagnostic_bundle_download_served: code-level-download-served`, `diagnostic_bundle_redaction_status: code-level-applied`, `diagnostic_bundle_authz_status: token-required-route-contract`, `diagnostic_bundle_retention_status: code-level-applied`, public trusted signing/external stable publication `not-claimed`다. Evidence는 `docs/ga-ready/evidence/diagnostic-bundle-server-code-level-2026-05-08.md`다.
- 2026-05-08 후속 diagnostic bundle listener code-level slice는 `DesktopNodeHostApplication` in-process listener에서 bearer-required create/download, `X-PCV-Request-Id` propagation, redacted `.bundle.json`, `X-PCV-Diagnostic-Bundle-Id` download header를 확인했다. Installed service listener execution, host mutation은 실행하지 않으며 `diagnostic_bundle_host_listener_execution: code-level-host-listener`, `diagnostic_bundle_installed_listener_execution: not-run`, `diagnostic_bundle_request_id_propagation: code-level-host-header`, public trusted signing/external stable publication `not-claimed`다. Evidence는 `docs/ga-ready/evidence/diagnostic-bundle-listener-code-level-2026-05-08.md`다.
- 2026-05-08 후속 diagnostic bundle product wrapper code-level slice는 `Invoke-PcvDesktopNodeProductAction -Action CollectDiagnostics`가 `New-PcvDesktopNodeDiagnosticBundle`로 위임되고 `product-wrapper-delegation-redacted.json`을 기록하는 것을 확인했다. `diagnostic_bundle_product_wrapper_delegation: code-level-product-action-orchestrator`, `actual_execution: code-level-product-wrapper`, `host_mutation_performed: false`, public trusted signing/external stable publication `not-claimed`다. 현재 자동화 shell은 non-elevated라 installed listener execution은 계속 `not-run`이다. Evidence는 `docs/ga-ready/evidence/diagnostic-bundle-product-wrapper-code-level-2026-05-08.md`다.
- 2026-05-08 후속 diagnostic bundle native service-action config code-level slice는 `DesktopNode.Host.exe service-action configure-installed|repair-installed` C# native SCM config가 `--diagnostics-root`, protected token file, route timeout, request limit, burst, retry-after 인자를 `BinaryPathName`에 포함하도록 보강했다. 0.38.9 installed final `PathName`에는 아직 이 인자들이 없었으므로 installed listener execution은 새 elevated rerun 전까지 `not-run`이다. Evidence는 `docs/ga-ready/evidence/diagnostic-bundle-native-service-action-config-code-level-2026-05-08.md`다.
- 2026-05-08 후속 timeout/rate-limit hardening preflight는 `packaging/windows-desktop-node/tools/New-PcvTimeoutRateLimitHardeningPreflight.ps1`를 추가해 service name, Local API route prefix, route timeout target, request limit target, retry-after target, UI/API error contract, Timeout and rate-limit hardening plan preview를 산출한다. Server config mutation, middleware enablement, retry semantics change, UI/API error behavior verification, load test execution, host mutation은 실행하지 않으며 `timeout_rate_limit_hardening: blocked-by-no-mutation-preflight`, `route_timeout_policy: not-applied`, `request_limit_policy: not-applied`, `retry_semantics_status: not-run`, `ui_api_error_contract_status: not-run`, `load_test_status: not-run`, `server_config_mutation: not-run`, `actual_execution: not-run`, `host_mutation_performed: false`, public trusted signing/external stable publication `not-claimed`다. Evidence는 `docs/ga-ready/evidence/timeout-rate-limit-hardening-preflight-2026-05-08.md`다.
- 2026-05-08 후속 timeout/rate-limit hardening code-level slice는 `DesktopNodeApiRequestProcessor`와 `DesktopNodeHostApplication`에 `/api/v1/` per-client request window, HTTP 429, `Retry-After`, `application/problem+json`, `PCV_RATE_LIMIT_EXCEEDED` contract를 추가했다. Route timeout enforcement, load test, server config mutation, installed service mutation, host mutation은 실행하지 않으며 `timeout_rate_limit_hardening: partial-code-level-request-limit`, `route_timeout_policy: not-applied`, `request_limit_policy: code-level-applied`, `retry_semantics_status: retry-after-problem-details-code-level`, `ui_api_error_contract_status: problem-details-json-code-level`, `load_test_status: not-run`, `server_config_mutation: not-run`, public trusted signing/external stable publication `not-claimed`다. Evidence는 `docs/ga-ready/evidence/timeout-rate-limit-hardening-code-level-2026-05-08.md`다.
- 2026-05-08 후속 timeout/rate-limit route-timeout code-level slice는 `DesktopNodeApiRequestProcessor`에 `/api/v1/` GET/read route response deadline, HTTP 504, `Retry-After`, `application/problem+json`, `PCV_ROUTE_TIMEOUT`, `route_timeout_seconds`, `request_id` contract를 추가했다. Mutation-route cancellation, native adapter cooperative cancellation, load test, server config mutation, installed service mutation, host mutation은 실행하지 않으며 `timeout_rate_limit_hardening: partial-code-level-route-and-request-limit`, `route_timeout_policy: code-level-applied`, `request_limit_policy: code-level-applied`, `retry_semantics_status: retry-after-problem-details-code-level`, `ui_api_error_contract_status: problem-details-json-code-level`, `load_test_status: not-run`, `server_config_mutation: not-run`, public trusted signing/external stable publication `not-claimed`다. Evidence는 `docs/ga-ready/evidence/timeout-rate-limit-hardening-route-timeout-code-level-2026-05-08.md`다.
- 2026-05-08 후속 timeout/rate-limit server-config code-level slice는 `PcvDesktopNodeProduct.psm1` service plan과 `DesktopNode.Host.exe service-action configure-installed|repair-installed` native SCM config에 `--route-timeout-seconds 30`, `--request-limit-per-minute 120`, `--request-burst-limit 20`, `--retry-after-seconds 15`를 추가했다. Installed service mutation, service stop/start, load test, host mutation은 실행하지 않으며 `timeout_rate_limit_hardening: partial-code-level-route-request-and-server-config`, `route_timeout_policy: code-level-applied`, `request_limit_policy: code-level-applied`, `retry_semantics_status: retry-after-problem-details-code-level`, `ui_api_error_contract_status: problem-details-json-code-level`, `load_test_status: not-run`, `server_config_mutation: code-level-product-and-native-service-plan-applied`, public trusted signing/external stable publication `not-claimed`다. Evidence는 `docs/ga-ready/evidence/timeout-rate-limit-hardening-server-config-code-level-2026-05-08.md`와 `docs/ga-ready/evidence/diagnostic-bundle-native-service-action-config-code-level-2026-05-08.md`다.
- 2026-05-08 후속 timeout/rate-limit load-test code-level slice는 `ApiHardeningRequestProcessorTests`에 같은 client identity 64개 in-process request load evidence를 추가했다. 결과는 HTTP 200 `20`, HTTP 429 `44`, unexpected status `0`이며 모든 429 응답은 `PCV_RATE_LIMIT_EXCEEDED`, `retry_after_seconds=9`, `operation=rate.limit` problem-details contract를 유지한다. Installed listener load, external load generator, service mutation, host mutation은 실행하지 않으며 `timeout_rate_limit_hardening: partial-code-level-route-request-server-config-and-load`, `route_timeout_policy: code-level-applied`, `request_limit_policy: code-level-applied`, `retry_semantics_status: retry-after-problem-details-code-level`, `ui_api_error_contract_status: problem-details-json-code-level`, `load_test_status: code-level-inprocess-pass`, `server_config_mutation: code-level-product-and-native-service-plan-applied`, public trusted signing/external stable publication `not-claimed`다. Evidence는 `docs/ga-ready/evidence/timeout-rate-limit-hardening-load-test-code-level-2026-05-08.md`다.
- 2026-05-07 후속 Burn bootstrapper preflight는 `packaging/windows-desktop-node/tools/New-PcvBurnBootstrapperPreflight.ps1`를 추가해 publication descriptor와 HTTPS MSI URL에서 WiX Burn authoring preview를 산출한다. `burn_bootstrapper: not-built`, `actual_execution: not-run`, `host_mutation_performed: false`, public trusted signing/external stable publication `not-claimed`이며 evidence는 `docs/ga-ready/evidence/burn-bootstrapper-preflight-2026-05-07.md`다.
- 2026-05-07 후속 MSIX packaging feasibility preflight는 `packaging/windows-desktop-node/tools/New-PcvMsixPackagingFeasibilityPreflight.ps1`를 추가해 MSIX package manifest preview를 산출한다. Service packaging design evidence가 없으므로 `msix: feasibility-blocked-by-service-packaging-design`, `actual_execution: not-run`, `host_mutation_performed: false`, public trusted signing/external stable publication `not-claimed`이며 evidence는 `docs/ga-ready/evidence/msix-packaging-feasibility-preflight-2026-05-07.md`다.
- 2026-05-07 `0.38.7-rc.1` internal enterprise `RequireSigned` MSI build는 `artifacts/internal-enterprise-requiresigned-rc-msi-20260507-0387`에서 Authenticode `Valid`, SignTool verify exit `0`, MSI SHA-256 `c44128cd84f6f3d93eabb8edb2a41930e4fbe16a081569a62473090d8f68f602`로 PASS했다. 같은 shell의 `0.38.7-admin-smoke` full admin host mutation gate와 update/rollback mutation attempt는 non-elevated shell에서 `PCV_BATCH_ADMIN_REQUIRED`, `sc.exe stop PureCVisorDesktopNode` exit `5`, `PCV_PRODUCT_SERVICE_STOP_TIMEOUT`으로 차단됐고 `host_mutation_performed=false`다. Blocked artifacts는 `artifacts/batch-runs/full-admin-host-mutation-gate-20260507-0387`와 `artifacts/product-update-rollback-mutation-20260507-0387`다. 최신 실제 full admin host mutation PASS evidence는 `0.38.9-admin-smoke`와 `artifacts/batch-runs/full-admin-host-mutation-gate-20260508-202255-0389`다. Evidence는 `docs/ga-ready/evidence/host-mutation-signed-build-attempt-2026-05-07-0387.md`다.
- 2026-05-07 Beta-0 Web Dashboard smoke는 Batch Supervisor `WebRegression` profile로 `artifacts/batch-runs/beta-web-dashboard-smoke-20260507-025743`에서 PASS했다. `web/tests` Pester 26 tests, `npm test --prefix web`, `npm run verify:parity --prefix web`, `node --check web/app.js`가 모두 통과했고 summary는 `ok=true`, `status=completed`, `total_steps=4`, `executed_steps=4`다. 이는 read-only/static fixture beta evidence이며 Hyper-V/service/MSI/firewall/trust-store/LAN/update mutation evidence가 아니다. Evidence는 `docs/ga-ready/evidence/beta-web-dashboard-smoke-2026-05-07.md`다.
- 2026-05-07 후속 Web Dashboard slice에서 `GET /api/v1/network/inventory` read-only route를 `Network` 화면으로 승격했다. Web Console은 source, read-only mutation mode, switch/default count, switch type/default/management OS/external adapter field를 표시하며 user-visible fixture와 browser fixture에 `Default Switch`/`fixture-ethernet` evidence를 추가했다. 이 slice는 Hyper-V switch/IP/firewall/service/MSI/trust-store/LAN/update mutation을 실행하지 않는다. Evidence는 `docs/ga-ready/evidence/web-console-network-inventory-view-2026-05-07.md`다.
- 2026-05-07 후속 Web Dashboard slice에서 `Troubleshooting` 화면에 Diagnostic Bundle operator handoff UI를 추가했다. Web Console은 기존 product wrapper `CollectDiagnostics` action, `%ProgramData%\PureCVisor\desktop-node\diagnostics` root, token value/Authorization header redaction boundary, `no host mutation` 상태를 표시하며 browser fixture가 이를 검증한다. 이 slice는 Web API bundle 생성/download, elevated wrapper 실행 대행, Hyper-V/service/MSI/firewall/trust-store/LAN/update mutation을 실행하지 않는다. Evidence는 `docs/ga-ready/evidence/web-console-diagnostic-bundle-ui-2026-05-07.md`다.
- 2026-05-07 후속 API/Web hardening slice에서 `GET /api/v1/jobs`에 bounded `limit`/`offset` pagination과 retention metadata를 추가했다. 기본 page는 `limit=50&offset=0`, 최대 limit은 200이며 terminal job은 최신 500개를 보존하고 active job은 보존한다. Web Console `Activity`는 첫 page와 `count`/`returned`/`next_offset`/retention summary를 표시한다. Timeout/rate-limit, token rotation/revoke UX, diagnostic bundle server-side collection/download action, host mutation은 이 slice 범위가 아니다. Evidence는 `docs/ga-ready/evidence/api-web-retention-pagination-hardening-2026-05-07.md`다.
- 2026-05-07 후속 Web Dashboard slice에서 `Troubleshooting` 화면에 Token Rotation operator UX를 추가했다. Web Console은 protected token file root, runtime policy token storage, browser token presence, `Clear browser token`, `rotation handoff`, `no service token mutation`, `no host mutation` 경계를 표시한다. Browser token clear는 Web Console 입력/세션 token만 지우며 service token file rotation/revoke, service restart, MSI/firewall/trust-store/LAN/update mutation은 실행하지 않는다. Evidence는 `docs/ga-ready/evidence/web-console-token-rotation-ux-2026-05-07.md`다.
- 2026-05-07 `0.38.8-admin-smoke` installed destructive update/rollback smoke 재실행은 `artifacts/product-update-rollback-mutation-20260507-0388-elevated-pass`에서 PASS했다. MSI SHA-256은 `163baa1df75b5810efa49d6347f482077421b1665f29a7adc2e501cdbc3a7564`, provenance commit은 `fd4f854646fc159d54f7578230f00c51f80e201f`다. Update는 `0.38.6-admin-smoke -> 0.38.8-admin-smoke`, health `200`, update journal `succeeded/health`였고 rollback은 current root를 `0.38.6-admin-smoke`로 복원하고 `0.38.8-admin-smoke`를 `DesktopNode.failed` diagnostics root로 보존했다. Final service는 `Running`, boot time unchanged, `host_mutation_performed=true`다. 최초 `artifacts/product-update-rollback-mutation-20260507-0388` non-elevated attempt는 blocked history다. Evidence는 `docs/ga-ready/evidence/product-update-rollback-mutation-2026-05-07-0388.md`이며 public trusted signing 또는 외부 stable publication evidence가 아니다.
- 검증 기대 결과는 `docs/DEVELOPMENT_VERIFICATION_POLICY.md`를 따른다.

## 다음 우선순위

1. 내부 전용 GA-ready 제품 런타임 유지
   - 설계 문서: `docs/superpowers/specs/2026-05-02-purecvisor-desktop-node-ga-ready-redesign-design.md`
   - 현재 결정: `PRODUCT_RUNTIME_PROMOTION_DECISION: ga-ready-product-runtime`
   - 배포 범위: `DESKTOP_NODE_SERVICE_DISTRIBUTION_DECISION: internal-only-service`
   - 현재 marker: `DESKTOP_NODE_GA_READY_REDESIGN_DECISION: powershell-free-product-ops-runtime`
   - ADR: `docs/adr/0004-ga-ready-product-runtime-candidate.md`
   - Route matrix: `docs/ga-ready/ROUTE_PROMOTION_MATRIX.md`
   - Repo migration map: `docs/ga-ready/REPO_MIGRATION_MAP.md`
   - Verification ownership map: `docs/ga-ready/VERIFICATION_OWNERSHIP.md`
- 첫 구현 slice는 ADR-0001 supersede 후보 범위 정의, route promotion matrix, repo migration map, verification ownership map을 문서/계약으로 고정했다.
- 2026-05-03 Web Console served asset/root migration slice는 `web/src/served-app.ts`가 served `web/app.js`를 생성하게 하고, Web Console 제품 경로를 repo-root `web/**`로 이동했다. 이 web slice 자체는 PowerShell helper 제거, Hyper-V route mutation adapter 전환, stable release 발행, MSI 관리자 mutation을 실행하지 않았다. 후속 VM power-state, checkpoint mutation, VM native lifecycle/delete slices에서 VM create/start/shutdown/poweroff/restart/delete와 checkpoint create/restore/delete는 C# native adapter로 전환됐다. 2026-05-05 aggregate closure는 GA-ready gate를 `closed`로 기록했다. 당시 재계산 값은 GA-scope blocked row 0개, PowerShell-backed current owner 0개, active product `spikes/**` reference 0개, future implementation exclusion 2개였다. 2026-05-07 `0.38.6-admin-smoke` 이후 현재 route matrix 재계산 값은 GA-scope `current-route` 18개, `product-operation` 24개, `future-route` exclusion 0개, `current-native` 42개, blocked/ga-ready-candidate 0개다. 2026-05-04 repo migration active path removal slices에서 route parity runner의 spike service module import, MSI installer payload spike staging, standalone product wrapper asset spike staging, post-reboot active spike command, docs required verification command direct spike path, product wrapper protected-token spike service module import를 제거했고, 2026-05-05 physical archive move로 source `spikes/purecvisor-desktop-node/**`는 absent가 됐다. Event Log source registration/removal과 firewall/trust-store 4개 row도 code-level native action으로 current owner를 `dotnet-native`로 전환했다. 이후 `artifacts/service-msi-hyperv-firewall-truststore-admin-mutation-20260504-2035-0330`에서 `0.33.0-admin-smoke` Service/MSI/Hyper-V mutation과 row-isolated firewall-only/trust-store-only actual mutation이 통과했고, `artifacts/os-mutation-gates-20260505-003459-0341`에서 `0.34.1-admin-smoke` native MSI/firewall/LAN/internal trust-store gate도 통과했다. 사용자 재승인 후 `artifacts/os-mutation-gates-20260505-033503-0354`에서 `0.35.4-admin-smoke` 실행 당시 HEAD MSI/firewall/LAN/internal trust-store gate를 fresh 실행했고 final restore는 internal signed stable `0.35.2`로 복구했다. 후속 `0.35.5-admin-smoke` evidence는 `artifacts/routeparity-service-msi-hyperv-admin-host-mutation-20260505-094809-0355`와 `artifacts/os-mutation-gates-20260505-101659-0355-final`에서 Hyper-V/MSI/firewall/LAN/Event Log/internal trust-store gate를 다시 확인했다. Internal stable `0.35.2`/`0.35.3` release/update/rollback execution은 `artifacts/stable-internal-release-update-rollback-20260505-015550-0352-0353`에서 pass다. 이 evidence들은 public trusted signing 또는 외부 stable publication evidence가 아니며, ADR-0004는 이 evidence를 근거로 내부 전용 서비스 current decision으로 적용됐다.

2. .NET/TypeScript mixed runtime transition 후속
   - Phase 25 문서: `docs/superpowers/specs/2026-04-30-purecvisor-desktop-node-phase25-dotnet-typescript-transition-design.md`, `docs/superpowers/plans/2026-04-30-purecvisor-desktop-node-phase25-dotnet-typescript-transition.md`
   - TypeScript Web Console 경계 문서: `docs/superpowers/specs/2026-04-30-purecvisor-desktop-node-phase25-typescript-web-console-boundary-scaffold-design.md`
   - .NET Host replacement 문서: `docs/superpowers/specs/2026-05-01-purecvisor-desktop-node-dotnet-windows-service-host-replacement-design.md`, `docs/superpowers/plans/2026-05-01-purecvisor-desktop-node-dotnet-windows-service-host-replacement.md`
   - 첫 slice인 `.NET contract mirror`는 `src/DesktopNode.Contracts/**`와 `src/DesktopNode.Contracts.Tests/**`에 추가됐다.
   - 두 번째 slice인 순수 .NET job state transition validator는 `src/DesktopNode.Runtime/**`와 `src/DesktopNode.Runtime.Tests/**`에 추가됐다.
   - 병렬 후속 slice인 .NET API host/handler adapter candidate contract는 `src/DesktopNode.Api/**`와 `src/DesktopNode.Api.Tests/**`에 추가됐다. 이어진 route parity slice에서 native read routes, queued VM/checkpoint lifecycle routes, job get/cancel/retry, JSON job store save/load/recovery를 .NET request processor에 구현했다. 2026-05-03 read-route helper fallback removal slice 이후 `network.inventory`, `vm.list`, `GET /api/v1/vms/{id}`, `GET /api/v1/vms/{id}/checkpoints`는 C# native adapter product path가 직접 처리한다. Native parity failure는 PowerShell helper fallback 없이 structured failure로 반환한다. VM create/start/shutdown/poweroff/restart/delete와 checkpoint create/restore/delete는 .NET request processor queue를 유지하되 C# WMI/native Hyper-V adapter가 직접 실행하며 route owner contract는 `dotnet-native`다. `GET /api/v1/runtime/policy`는 native probe operation 목록, native mutation operation 목록, `native-vm-create-lifecycle-delete-checkpoint-mutation` mutation dispatch marker를 노출한다. PowerShell Local API는 계속 component 기준과 adapter 검증 경계로 유지한다.
   - 두 번째 병렬 후속 slice인 .NET Service host/lifecycle adapter contract는 `src/DesktopNode.Service/**`와 `src/DesktopNode.Service.Tests/**`에 추가됐고, replacement slice 이후 기본 owner는 `dotnet-windows-service-host`다.
   - PowerShell Hyper-V helper와 packaging 운영 runbook은 유지한다.
- TypeScript Web Console은 `web/src/served-app.ts`가 served `web/app.js`를 생성하는 static asset flow로 승격됐고, user-visible fixture source, generated parity manifest, `build:served`/`check:served`/`generate:parity`/`verify:parity`/`browser:fixture` flow를 `web/package.json`, `tsconfig.json`, `src/**`, `generated/parity/**`, `scripts/**`에 둔다.
   - 제품 실행 경로 교체는 `GET /api/v1/runtime/policy`의 순수 .NET request processor로 시작했고, 후속 slice에서 listener, port bind, WinSW 기본 실행 경로, MSI installed custom action을 `DesktopNode.Host.exe`로 교체했다.
   - service mutation 경계는 product wrapper process command guard에 `PCV_PRODUCT_AUTO_REBOOT_FORBIDDEN` no-auto-reboot 차단을 추가하는 방식으로 시작했다.
   - MSI lifecycle 경계는 기존 `REBOOT=ReallySuppress`, `MSIRESTARTMANAGERCONTROL=Disable`, `/norestart`, `1641` failure classification에 `no_auto_reboot` plan metadata를 추가하는 방식으로 시작했다.
   - 사용자 관리자 opt-in으로 `artifacts/product-path-service-msi-start-20260501-194840`에서 `0.26.0-admin-smoke` unsigned MSI를 빌드하고 실제 service install/start/stop/delete 및 MSI install/repair/uninstall/`REMOVE_DATA=1` lifecycle start smoke를 실행했다. 유효 evidence는 `lifecycle-sequential-20260501-200211`이며 install, repair, 기본 uninstall, remove-data reinstall/uninstall, restore install이 모두 exit `0`, service `Running`, Web root `200`, automatic reboot/reboot required 미관측으로 끝났다.
   - `artifacts/dotnet-host-admin-smoke-20260501-213444`에서 최신 .NET Host replacement build, direct service-action install/start/delete, MSI install/repair/uninstall/`REMOVE_DATA=1`/final restore, Hyper-V helper integration smoke를 실행했다. 최종 service는 `DesktopNode.Host.exe listen` 경로로 `Running`이며 자동 reboot는 없었다.
   - 같은 artifact root의 초기 `lifecycle/` runner 산출물은 `msiexec` 대기 방식 오류로 1618 경합을 만들었으므로 유효 lifecycle evidence로 사용하지 않는다.
   - 최신 설치본 mutation evidence는 `artifacts/routeparity-service-msi-hyperv-vm-delete-mutation-20260503-0301`과 `artifacts/guest-shutdown-windows-smoke-20260503-222750`에 기록됐다. `0.30.1-admin-smoke`는 service-action, MSI install/repair/uninstall/`REMOVE_DATA=1`/final restore, installed Hyper-V VM/checkpoint route smoke, VM create/start/restart/poweroff/delete native lifecycle adapter, checkpoint create/restore/delete native mutation adapter, runtime policy dispatch boundary 확인을 자동 reboot 없이 통과했고 `pcv-spike-*` VM 잔여물 없이 끝났다. Managed VM delete는 `action=delete`, repeat delete는 `action=absent`, unmanaged VM delete는 `PCV_VM_NOT_MANAGED_BY_PURECVISOR` blocked로 확인했다. Installer-ISO guest shutdown unavailable case는 `PCV_VM_SHUTDOWN_NOT_AVAILABLE` structured failure로 확인했다. Successful guest shutdown smoke는 Microsoft Windows Server 2022 Evaluation VHD guest에서 installed Local API `vm.shutdown` job `succeeded`, final VM `Off`, test VM/ProgramData smoke root cleanup 완료를 확인했다.
   - 이후 service status/start/stop C# product operation slice는 `artifacts/service-action-status-start-stop-20260504-002359` installed service mutation smoke까지 완료했다. `configure-installed|repair-installed|remove-installed|data-root-remove`는 native SCM/data-root action과 fake-controller tests를 통과했고, `artifacts/routeparity-service-msi-hyperv-data-root-handoff-20260504-032646-0303`의 `0.30.3-admin-smoke`에서 installed destructive service create/configure/repair/delete/remove-data와 MSI `REMOVE_DATA=1` lifecycle도 PASS로 확인했다. 이 smoke는 service 존재 중 `data-root-remove --remove-data` 차단, `remove-installed --remove-data` handoff-only, service absent 이후 allowlist data-root deletion, non-allowlist log 보존, final service `Running`, boot time unchanged, `pcv-spike-*` VM 잔여물 없음으로 끝났다. Route parity runner와 product wrapper protected-token path는 더 이상 spike service module import에 의존하지 않는다. MSI installer payload도 active `spikes/**` source 없이 product-owned payload만 stage한다. 사용자 관리자/host mutation opt-in으로 `artifacts/routeparity-service-msi-hyperv-admin-host-mutation-20260504-1412-0310`의 `0.31.0-admin-smoke`를 실행해 repo migration active path removal 이후 service-action, MSI install/repair/uninstall/`REMOVE_DATA=1`/final restore, installed Hyper-V API route smoke를 product-owned MSI payload로 재확인했다. Build commit은 `ac26cf9a8b355de4984536b3bb5492979719f6b7`, final service는 `DesktopNode.Host.exe` 경로 `Running`, boot time unchanged, `pcv-spike-*` VM 잔여물 없음이다. 이후 `artifacts/stable-internal-release-update-rollback-20260505-015550-0352-0353`에서 internal stable `0.35.2` install/repair/uninstall/remove-data, `0.35.3` local payload update, `0.35.2` rollback restore도 pass로 확인했다. 이전 설치본 lifecycle/route evidence는 각 artifact에 남아 있고, `AllowUnsignedDev` 또는 internal trust evidence이므로 public trusted signing이나 외부 stable publication evidence가 아니다.
- standalone product asset boundary 후속 slice 이후 사용자 관리자/host mutation opt-in으로 `artifacts/routeparity-service-msi-hyperv-admin-host-mutation-20260504-1515-0320`의 `0.32.0-admin-smoke`를 실행해 service-action, MSI install/repair/uninstall/`REMOVE_DATA=1`/final restore, installed Hyper-V API route smoke를 active `spikes/**` product asset 없이 재확인했다. Build commit은 `d852ff54bafb403e16e86057b3cecec2813bf0b6`, MSI SHA-256은 `f3e4456e94d5ee16a8e0bd6d02d17ac04d682be5bd58c77098072f97711d25f5`, payload file count는 7, final service는 `DesktopNode.Host.exe` 경로 `Running`, boot time unchanged, `pcv-spike-*` VM 잔여물 없음이다. Managed VM delete는 `action=delete`, repeat delete는 `action=absent`, unmanaged guard는 `PCV_VM_NOT_MANAGED_BY_PURECVISOR`로 유지됐다. 후속 사용자 관리자 opt-in으로 `artifacts/service-msi-hyperv-firewall-truststore-admin-mutation-20260504-2035-0330`의 `0.33.0-admin-smoke`도 실행해 Service/MSI/Hyper-V mutation을 `dca492c67c0cb3843832d5f6e1e76c8d686c3cdf`에서 재확인했고, firewall-only smoke는 owned inbound allow rule create/enable/remove 후 final rule count 0, trust-store-only smoke는 test cert LocalMachine Root/TrustedPublisher import/remove 후 final absence를 확인했다. 이후 fast-mode 관리자 opt-in으로 `artifacts/os-mutation-gates-20260505-003459-0341`의 `0.34.1-admin-smoke` native gate를 실행했다. 사용자 재승인 후 `artifacts/os-mutation-gates-20260505-033503-0354`의 `0.35.4-admin-smoke` 실행 당시 HEAD native gate도 fresh 실행했다. `0.35.4` MSI provenance commit은 `744a15536569e89f948927bea9179fc0eeae3ff4`, MSI SHA-256은 `bf7d0d2bd83545e83fbdf0dfb96b715f8e09471474445ae1c0db1d076be2c1e4`이며 MSI lifecycle, native firewall enable/remove, LAN IP runtime policy와 Web root `HTTP 200`, ADR-0003 internal Root/TrustedPublisher install/remove/restore, internal signed stable `0.35.2` final restore를 확인했다. 후속 `0.35.5-admin-smoke`는 최신 OS mutation gate로 Event Log register/remove와 LAN IP `http://[redacted-private-endpoint]:7777/` Web assets `HTTP 200`까지 확인했다. 이 evidence들은 unsigned `AllowUnsignedDev` admin-smoke, scoped test certificate mutation, 또는 ADR-0003 internal trust-store evidence이므로 public trusted signing 또는 외부 stable publication evidence가 아니며, 제품 런타임 승격 판단은 2026-05-05 aggregate closure와 ADR-0004가 소유한다.
   - 기본 검증은 `dotnet test src/DesktopNode.sln`, `npm test --prefix web`, `npm run generate:parity --prefix web`, `npm run verify:parity --prefix web`, `npm run browser:fixture --prefix web`, 관련 Pester suite, `git diff --check`다.

3. Local API job runtime boundary baseline 유지
   - Phase 24 문서: `docs/superpowers/specs/2026-04-30-purecvisor-desktop-node-phase24-local-api-job-runtime-boundary-design.md`, `docs/superpowers/plans/2026-04-30-purecvisor-desktop-node-phase24-local-api-job-runtime-boundary.md`
   - 계획된 TDD/CLI/diagnostics slice는 완료됐고, `GET /api/v1/runtime/policy`의 `job_runtime` contract, job state, persistence, worker dispatch, host mutation boundary는 현재 baseline으로 유지한다.
   - CLI는 `runtime policy` 명령으로 이 public contract를 직접 조회한다.
   - PowerShell 구현을 바로 C++23으로 옮기지 않는다. Public contract가 안정화된 뒤 C++23 core 후보를 다시 판단한다.
   - 기본 검증은 Local API/CLI Pester suite, Desktop Node root boundary suite, `git diff --check`다.

4. Internal stable service boundary
   - Phase 20 문서: `docs/superpowers/specs/2026-04-29-purecvisor-desktop-node-phase20-signed-release-msi-lifecycle-evidence-design.md`, `docs/superpowers/plans/2026-04-29-purecvisor-desktop-node-phase20-signed-release-msi-lifecycle-evidence.md`
   - `0.23.8-rc.1` signed RC MSI evidence와 elevated MSI lifecycle exit `0` smoke는 local test certificate로 수집됐다.
   - `artifacts/p0-local-requiresigned-rc-msi-20260501-165251`에서 current-head `3d35aa2` 기준 `0.23.9-rc.1` local test `RequireSigned` MSI build, elevated MSI lifecycle 전부 exit `0`, product-wrapper update/rollback/config migration smoke, final MSI restore install을 재확인했다.
   - `artifacts/internal-enterprise-requiresigned-rc-msi-20260501-181021`에서 current-head `318ebc3` 기준 `0.23.10-rc.1` internal enterprise `RequireSigned` MSI build, LocalMachine trust import, Authenticode `Valid`, SignTool verify exit `0`, elevated MSI lifecycle PASS를 확인했다.
   - `artifacts/admin-optin-hyperv-service-msi-firewall-eventlog-20260501-185911`에서 같은 `0.23.10-rc.1` internal enterprise MSI로 install/repair/uninstall/install-remove-data/uninstall-remove-data/final restore가 모두 성공했고, 자동 reboot는 비활성/미관측이었다.
   - `artifacts/product-path-service-msi-start-20260501-194840/lifecycle-sequential-20260501-200211`에서 `0.26.0-admin-smoke` unsigned MSI lifecycle start smoke도 모두 exit `0`으로 끝났지만, signing mode가 `AllowUnsignedDev`이므로 public trusted signing 또는 외부 stable publication evidence가 아니다.
   - `artifacts/dotnet-host-admin-smoke-20260501-213444`에서 `0.26.0-admin-smoke` .NET Host replacement MSI lifecycle과 service-action smoke도 통과했지만, signing mode가 `AllowUnsignedDev`이므로 public trusted signing 또는 외부 stable publication evidence가 아니다.
   - `artifacts/routeparity-service-msi-hyperv-mutation-20260502-012126`에서 `0.26.8-admin-smoke` 설치본 .NET Host route parity smoke와 native `network.inventory` response도 service/MSI/Hyper-V mutation까지 통과했지만, signing mode가 `AllowUnsignedDev`이므로 public trusted signing 또는 외부 stable publication evidence가 아니다.
   - `artifacts/p1-release-approval-and-signing-preflight-20260430-2045`에서 public trusted code-signing cert/PFX 후보가 없음을 기록했고, stable unsigned build 차단은 유지됨을 확인했다.
   - `artifacts/p0-local-root-trust-workaround-20260430-2120`에서 test signer를 `Cert:\CurrentUser\Root`와 `Cert:\CurrentUser\TrustedPublisher`에만 추가해 Authenticode `Valid`, SignTool verify exit `0`을 확인했다. 이 항목은 public trusted signing이 아니라 로컬 검증 우회다.
   - 사용자 명시 release approval로 draft-ready release signing gate를 닫았다.
   - 2026-05-05 internal stable `0.35.2`/`0.35.3` release/update/rollback evidence는 pass다.
   - 외부 배포, public trusted Authenticode signing, 일반 사용자 public stable publication은 내부 전용 서비스 scope 밖이며 별도 ADR 없이는 진행하지 않는다.

5. Security/operations cleanup record
   - Ready gate 문서: `docs/superpowers/plans/2026-04-30-purecvisor-desktop-node-draft-pr-ready-gate.md`
   - security/ops evidence: `artifacts/p2-security-ops-check-20260430-192500`
   - firewall cleanup evidence: `artifacts/p2-firewall-smoke-rule-removal-20260430-2030`
   - service/token/listener/ACL은 기대값이다.
   - 사용자 명시 승인 후 enabled inbound rule `PureCVisor Desktop Node API Smoke`를 제거했고 after count `0`을 확인했다.
   - 2026-05-01 관리자 opt-in hardening evidence는 `artifacts/admin-optin-continuation-20260501-162940`, WinSW wrapper restore evidence는 `artifacts/admin-optin-service-wrapper-restore-20260501-162904`, TLS reverse proxy preview evidence는 `artifacts/admin-optin-tls-reverse-proxy-preview-20260501-163308`에 기록됐다.
   - 최신 보강 evidence는 `artifacts/admin-optin-hyperv-service-msi-firewall-eventlog-20260501-185911`에 기록됐다. final service failure action은 `final/sc-qfailure-final.out.txt`에서 restart/restart 정책으로 조회됐다.
   - .NET Host replacement evidence는 `artifacts/dotnet-host-admin-smoke-20260501-213444`에 기록됐다. final service path는 `DesktopNode.Host.exe listen --prefix http://127.0.0.1:7777/ ... --api-token-protected-file ...`다.
   - 최종 상태는 service `Running`, runtime policy ok, host status ok, exposure `loopback`, token storage `dpapi-local-machine`, test VM/firewall/listener 잔여 0이다.

6. Hyper-V 수명주기 통합 증거 기록 유지
   - Phase 21 문서: `docs/superpowers/specs/2026-04-29-purecvisor-desktop-node-phase21-hyperv-lifecycle-integration-evidence-design.md`, `docs/superpowers/plans/2026-04-29-purecvisor-desktop-node-phase21-hyperv-lifecycle-integration-evidence.md`
   - product-flow rerun은 `artifacts/phase21-product-flow-rerun-20260430-190840`에 기록됐다.
   - 최신 보강 evidence `artifacts/admin-optin-hyperv-service-msi-firewall-eventlog-20260501-185911`에서 VM create/start/checkpoint list/poweroff/remove가 성공했고 checkpoint list와 direct snapshot evidence 모두 checkpoint 이름을 확인했다. 이전 checkpoint list 누락 이슈는 닫혔다.
   - .NET Host replacement run의 Hyper-V evidence `artifacts/dotnet-host-admin-smoke-20260501-213444/hyperv-integration-smoke.json`은 helper runner integration smoke다. 이후 코드 slice에서 .NET Host route parity 시작 범위를 request processor와 Host body/helper/job-store forwarding으로 구현했고, `artifacts/routeparity-service-msi-hyperv-mutation-20260502-012126/hyperv-api-route-smoke.json`에서 설치본 `host.status`, native `network.inventory`, VM lifecycle, checkpoint create/list/delete route smoke가 통과했다.
   - failure retry는 current contract상 `409/PCV_JOB_NOT_RETRYABLE`로 기록한다.

7. 릴리스/버전 정책 안정 채널 발행 게이트
   - Phase 22 문서: `docs/superpowers/specs/2026-04-29-purecvisor-desktop-node-phase22-release-version-policy-design.md`, `docs/superpowers/plans/2026-04-29-purecvisor-desktop-node-phase22-release-version-policy.md`
   - dev/admin-smoke/rc/stable 채널 정책과 산출물 명명 규칙은 정의됐고, 설치기 출력과 출처 증명 계약은 일부 강제됐다.
   - Phase 22 정책과 설치기 산출물/채널 계약은 ADR-0002로 현재 적용 결정에 채택됐다.
   - 내부 stable release/update/rollback은 selected trust model, update compatibility, rollback final-state proof를 `artifacts/stable-internal-release-update-rollback-20260505-015550-0352-0353`에서 확인했다. 이 evidence는 public stable publication 승인이 아니다.
   - GA-ready 제품 런타임 승격은 ADR-0004로 내부 전용 서비스 범위에서 적용됐다.

8. Windows 운영 증거
   - Phase 23 문서: `docs/superpowers/specs/2026-04-29-purecvisor-desktop-node-phase23-windows-operational-evidence-design.md`, `docs/superpowers/plans/2026-04-29-purecvisor-desktop-node-phase23-windows-operational-evidence.md`
   - `artifacts/p1-operational-eventlog-lifecycle-20260430-2050`에서 기존 service Running 관측, ProductStatus/CollectDiagnostics exit `0`, Web root `200`, Event Log source register/write/read/remove lifecycle을 기록했다.
   - `artifacts/admin-optin-continuation-20260501-162940`에서 service failure action 실제 적용, service lifecycle, firewall, Event Log scoped source, LAN listener/firewall, direct Hyper-V, Product API Hyper-V, 75초 운영 log evidence를 추가로 기록했다.
   - `artifacts/admin-optin-hyperv-service-msi-firewall-eventlog-20260501-185911`에서 service `Running`, Web root `200`, final failure action 조회, firewall smoke rule cleanup, Event Log source write/read/remove, 자동 reboot 비활성/미관측을 재확인했다.
   - `artifacts/dotnet-host-admin-smoke-20260501-213444`에서 기본 service host가 .NET Host로 전환된 후 service/MSI/Hyper-V helper smoke를 재확인했다.
   - `artifacts/admin-optin-tls-reverse-proxy-preview-20260501-163308`에서 trust store 변경 없이 self-signed TLS reverse proxy preview를 통과했고 private key cleanup을 확인했다.
   - JSONL-first는 primary로 유지하고 Event Log writer/provider 기본 활성화는 후속 설계 판단으로 남긴다.
   - 결과는 Phase 23 plan의 완료 증거에 기록한다.

9. ADR 운영 후속
   - 새 Phase가 공개 경계, 제품 승격 게이트, installer/service/update/security 정책을 바꾸면 `docs/adr/`에 새 ADR을 추가하거나 기존 ADR을 대체한다.
   - 다음 GA 차단 게이트가 실제 증거로 닫히면 `docs/ADR_INDEX.md`와 관련 ADR의 상태를 함께 갱신한다.
   - Phase 11-19 전체를 소급 개별 ADR로 분해하지 않는다. 현재는 `ADR-0001`, `ADR-0002`, `ADR-0003`을 현재 적용 결정의 진입점으로 유지한다.

10. Repo-root 정리
   - 현재는 phase 이력 보존을 위해 `archive/spikes/purecvisor-desktop-node/**` 경로를 유지한다.
   - 후속 PR에서 `src/api`, `src/cli`, `src/hyperv`, `src/service`, `web`, `packaging/windows-desktop-node` 같은 제품 repo 구조로 이동할지 결정한다.
   - 경로 이동은 product wrapper `SourceRoot` 계약과 Pester suite를 함께 갱신할 때만 수행한다.

11. Draft PR ready 전환 판단
   - Ready gate 문서: `docs/superpowers/plans/2026-04-30-purecvisor-desktop-node-draft-pr-ready-gate.md`
   - direct draft-ready evidence가 닫혔으므로 PR 본문을 최신 head와 evidence로 갱신한 뒤 ready 전환을 판단한다.

## 기본 검증 후보

```powershell
pwsh -NoProfile -Command "Invoke-Pester -Path 'packaging/windows-desktop-node/tests' -Output Detailed"
pwsh -NoProfile -Command "Invoke-Pester -Path 'packaging/windows-desktop-node/installer/tests' -Output Detailed"
pwsh -NoProfile -Command "Invoke-Pester -Path 'web/tests' -Output Detailed"
dotnet test src/DesktopNode.sln
npm test --prefix web
npm run generate:parity --prefix web
npm run verify:parity --prefix web
npm run browser:fixture --prefix web
node --check web/app.js
git diff --check
```

Component/archive baseline 검증은 verification ownership map으로 분리하며, 기본 검증 후보에는 active `spikes/**` Pester path를 넣지 않는다.
